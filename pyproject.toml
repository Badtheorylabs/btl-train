[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "btl-train"
version = "0.1.0"
description = "BTL training backend inspection and planning"
license = {file = "LICENSE"}
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["btl_train*"]
